# 路线图

[English](ROADMAP.md) | 中文

这是产品方向，而不是带日期的功能清单。dshline 是一个**理解 Harness 能力而非重新实现它们的 DeepSeek Harness 终端原生前端。**它的架构规则是：

> **Harness 拥有能力；dshline 只负责终端呈现。**

证明很简单：安装一个新的 Harness 提供方或插件；它发布一个已有的标准能力接口；dshline 已经知道如何呈现那个接口。真实的 Codex 提供方已经通过 `ctx.subagents` 与 `ctx.jobs` 的验收进入通用 Work，无需任何 Codex 专用的 dshline 集成。Provider 的配置也遵循同一条规则：`/connect` 呈现 `ctx.llm` 的可配置提供方目录、`ctx.settings`、`ctx.credentials` 与 `ctx.authorization` 发布的任何内容，没有自己的提供方注册表，也没有登录协议。[Provider 验收](docs/provider-acceptance.md)记录了证据、配置边界与下一个目标。原生终端滚动缓冲区（scrollback）是另一个差异化优势。这里不是在声称其他 TUI 不能扩展；它描述的是本前端正在选择的架构。

## 产品原则

- **原生终端滚动缓冲区是一条不变量。**完成后的会话记录行提交到用户真实的终端，绝不被虚拟化、重写或移到备用屏幕。只有有界的活动区域被重绘，因此正常的终端选择、复制与滚动回退（scrollback）始终可用。
- **渲染器保持 Harness 无关且无依赖。**它了解终端文本、宽度、按键、边框盒与「追加 + 活动区域」屏幕；Harness 接线属于它的上层。
- **产品集成之前，先使用标准 Harness 权威。**优先使用 `ctx.subagents`、`ctx.jobs`、`ctx.llm`、`ctx.commands`、`ctx.tools`、`ctx.sessionQuery`、`ctx.attachments` 与 `ctx.sessionProjections`，而不是提供方连接、解析输出或重复的数据库。
- **能力保持可选。**没有某个服务的配置文件仍然能启动；相关的视图显示为不可用，而不是成为启动失败。
- **观察与控制是分离的约定。**可调用的变更不自动是安全的人类操作。人类 UI 需要其所有者明确的生命周期、授权、调度与模型通知语义。
- **没有静默降级。**显示 Harness 暴露的结构化事实；当接口回答不了问题时，少说，而不是从文本猜测。
- **未来的抽象仍然产生有界的行。**声明式视图代码只有在最终服务于现有 `Screen`/`TuiSlots` 终端模型时才是受欢迎的，而不是作为全屏替代。

## 能力方向

各领域可以独立推进，但预期顺序如下。

### 1. Harness Work——已合并

第一个通用能力适配器通过有界的 Work UI 呈现来自 `ctx.jobs` 的后台任务与来自 `ctx.subagents` 的委派活动。

- 观察任务而不消费它们的输出游标
- 观察 subagent 并保留 Harness 的提供方无关生命周期事实
- 把真实提供方支持当作上游约定验收测试，而不是提供方专用的 dshline 集成
- 把已完成的 Codex 验收与未验证的 Claude Code 目标分开记录；两者都不需要提供方专用的 dshline 生产代码

在 Harness 发布权威关联之前，任务与 subagent 保持分离。Codex 验收已完成；通过 `@deepseek-ai/dsh-subagent-claude-code`、`ctx.subagents` 与 `ctx.jobs` 的 Claude Code 是下一个验收目标，而不是已手动验证的集成。见 [Provider 验收](docs/provider-acceptance.md)。Work 还确立了更广的控制规则：它不暴露 `ctx.jobs.kill()`，因为该方法具有面向模型的「已报告交付」语义；可续的 subagent 只有在 `ctx.subagents` 明确建模时，才能暴露用户授权的中断。

### 2. 会话投影与 agent 状态

`ctx.sessionProjections` 是第二个被验证的架构模式。领域插件注册投影单元；Harness 拥有它们的日志驱动、缓存、快照与变更流；dshline 把快照与变更作为呈现输入消费。其内部共享观察器为原生适配器供数，而不是让每个特性重放同一个会话日志。

1. **Todo——已实现。**`/todos` 与一条紧凑状态读数通过一个内部会话作用域观察器，消费 `@deepseek-ai/dsh-tool-todo` 提供的 `todos` 投影。整列表的 `todo/write` 状态、生命周期与持久化仍归 Harness 所有；dshline 既不解析调用、卡片或工具输出，也不拥有任何 Todo 状态机。
2. **下一步，打磨 Goal。**把持久的 `goal` 投影用于日志派生的状态，在需要实时、进程局部的续跑权威时使用 `ctx.goals`。
3. **Plan 仍由 Harness 治理。**它的呈现继续遵循计划能力文档化的权威，而不是 TUI 自有的镜像。

不承诺任何通用的公共 `ProjectionAdapter` 接口。

### 3. Sessions——已合并

第三个通用能力适配器仅通过 `ctx.sessionQuery` 呈现 Harness 会话语料库。`/sessions` 与 `--resume` 打开同一个有界浏览器：它列出偏好活动的记录并批量折叠标题、随输入过滤、在 `tab` 时把同样的词交给引擎的全文接口，并在原位重新打开一个会话。

- 用 `listSessions()`、`readTitleSnapshots()` 与 `listEvents()` 读取语料库；绝不扫描会话目录或保留第二个索引
- 把 `searchSessions()` 视为可选：它是引擎唯一抽象接口，报告 `SESSION_QUERY_SEARCH_DISABLED` 的后端降级为过滤而不是失败
- 通过拥有的 `AgentHandle` 处置器与 `ctx.agents.resume` 重新打开，并在 Harness 未定义生命周期的状态下拒绝
- 把会话记录保留在原生滚动缓冲区中：重新打开是追加，绝不重写

它还引入了窗口/附件拆分——窗口拥有终端、按键路由、模型路由与阅读偏好，而附件拥有一个 agent 及其日志投影出的一切。这个拆分是可复用的部分；任何为整个会话替换领域状态的未来能力都需要它。

Sessions 仍在前面：

- 通过 `readEvent()` 检视会话内搜索命中的上下文
- 重命名一个已关闭的持久会话，前提是 Harness 出现更窄的变更接口——通用标题服务只操作活动会话对象
- 已知工作区列表与"近期活动"过滤，前提是 Harness 将来为它们发布谓词

Sessions 2.0 通过同一条 seam 交付了最初的清单，没有第二个索引，也没有前端自有的会话状态：

- 通过 `filterSessions()` 的语料库过滤：工作区（`cwd` 精确匹配）与年龄（`created-at` 闭区间窗口）
  成为 Harness 子句；来源（`own`/`delegated`）仅在呈现层应用，依据 Harness 返回的权威观测头部
  分类——当搜索后端自己的命中投影省略它时，通过批量标题观测恢复——因为 Harness 不发布任何来源谓词
- 通过 `traceSession()` 的血统导航，投影为有界树（祖先向外、后代深度优先），带精确的裁剪计数，
  并在父代离开可见语料库时给出诚实的一行
- 对所选会话通过 `searchEvents()` 做会话内搜索
- 两个全文作用域都支持真正的游标分页：绑定到确切请求的不透明 Harness 游标、显式的 `Load more…`
  行，以及语料库在游标之下变动时的刷新路径
- 通过 `ctx.sessionTitle.rename` 重命名本窗口正在驱动的会话——一个显式的 `user` 来源标题，钉住
  该会话的标题；重命名一个已关闭的持久会话仍然推迟，因为通用服务只操作活动会话对象

### 4. Connect——已合并

第四个通用能力适配器呈现提供方的配置，而这是四个 Harness 接口而不是一个。`/connect` 把 `ctx.llm` 的可配置提供方目录与已注册路由、`ctx.settings` 的用户设置文档、`ctx.credentials` 的凭据存在性与 `ctx.authorization` 上注册的登录流程连接起来，呈现为一个有界浏览器。

- 用 `listConfigurableProviders()` 读取目录，这样裸挂载适配器的整个已安装目录在任何路由存在之前就被提供；绝不发布提供方列表
- 通过 `describe()` 按 schemastery 的 `credential-ref` 角色找到配置文件的凭据字段，绝不按本前端认识的字段名
- 只通过 `ctx.credentials` 写入密钥，设置只作为携带读取时修订号（revision）的路径操作
- 通用地渲染 `ctx.authorization` 的中立通知与提示词汇，不实现任何提供方的登录；通知进入原生滚动缓冲区，因为登录 URL 与设备码生来就是要被复制的
- 保持提供方与登录分区分离，因为 Harness 不发布凭据记录与提供方路由之间的关联——Work 对任务与 subagent 也是同样的拒绝

由于两个接口写入同一个命名空间与同一个引用，终端里做出的变更在官方 Web Models 页可见，反之亦然；`/model` 无需进一步步骤就能看到新激活路由的模型。`/model` 保持本来的样子：在已存在的模型中选择。

Connect 2.0 关闭了这份清单中的大部分。`/connect` 现在可以声明所有者适配器未发布任何内容的路由——网关、自托管服务器、本地 OpenAI 兼容端点——通过今天唯一一个其设置配置文件能够描述整条提供方路由的命名空间 `llm-pi-ai`。这份知识只存在于一个小型呈现模块（`connect/pi-ai.ts`）中：它从该命名空间自身的序列化 schema 读取协议选项，并通过其他每个 Connect 动作已经使用的同一套通用 `ctx.settings` 路径操作写入；它在运行时不导入任何 pi-ai 运行时代码，也不发起任何网络请求。端点探查通过 `ctx.llm.discoverModels()` 完成——这是供读者挑选的建议性候选项，绝不会被自动写入——而 Connect 现在除 `llm/adapters-updated` 外，还收敛 `settings/updated`、`settings/document-updated`、`credentials/reference-updated` 与 `credentials/record-updated`，因此从官方 Web Models 页所做的修改或手工编辑的 `settings.yaml` 都不再需要 `ctrl-r`。

Connect 仍在前面：

- 开放 pi-ai 更进阶的 `compat`、请求头、重试策略与逐模型的推理字段，这些目前仍留在 `settings.yaml` 中；Connect 2.0 精选的是决定读者能触达哪个路由/模型的字段，而不是整份配置文件
- 除 `llm-pi-ai` 之外的第二个命名空间获得同样的「声明整条路由」能力，那将需要它自己的小型呈现模块，而不是一个通用模块——原因见
  [架构 → Connect 2.0](docs/architecture.zh.md#connect-20-one-route-can-be-a-declaration-not-only-a-lookup)

### 5. Agent 预设——已合并

第五个通用能力适配器呈现 agent 的 COMPOSITION（组合）：运行中的 agent 实际拥有哪些工具、
提示词分节与委派后端，通过 `ctx.agentPresets` 读取。`/plugins` 浏览名册以及某个 agent 所加入
预设的组合，并把每一项变更交给拥有它的 seam 执行。

- 通过 `list()`/`read()` 读取名册与某个预设的组合；绝不使用私有插件注册表，也绝不从工具名或
  渲染输出推断
- 只在本地编写的预设上切换某一行——内置预设先复制（`copy()`），绝不就地编辑——并通过 Harness
  自己的健康检查重新校验结果，而不是私自重新解析
- 只通过 `mount()` 加入某个 agent 的组合，只通过 `select()` 切换它——那是 Harness 自己的完整
  操作：在它自己串行化的切换内部重新检查权威的 `turnBoundary` 投影、拒绝已开始的会话、重新
  组合，并记录这次选择。dshline 自己不执行其中任何一步；已开始的会话其预设是固定的，终端改为
  提供*下一个*会话的默认项
- 按 Harness 的 `agentPreset` 投影所报告的预设恢复会话，绝不用今天的名册默认值——而在本适配器出现之前产生、
  因而没有记录任何预设的会话，按随附的 `standard` 预设恢复，而不是任意一个当前预设；未提供
  可用 `standard` 的部署回退到它自己的默认值并在 transcript（文本记录）中说明，而不是拒绝
  打开自己的历史
- 采纳这一点意味着把 dshline 自己原先进程级的工具集移到 Harness 的 Web 前端已经使用的同一条
  预设边界之后，与"agent 平面移到 agent 预设之后"是同一步，理由也完全相同

`/profiles` 呈现其上的配置文件层：通过 Harness 自己的 `dshHomePath` 服务读取 `$DSH_HOME/profiles`
下的名册，从 Loader 的 base URL 读取已启动的配置文件，以及每个配置文件的 `dsh.profile.bundles`
层——凡是 pnpm 的状态已经记录了安装版本的地方都一并给出。它的变更被转发给 `dsh plugin --profile
<name> …`——连同 Harness 自己的包生命周期、pnpm 调用与 bundle 协调——因此这个适配器不添加任何
安装器、解析器或 lockfile 行为，`/plugins` 也依然不自建包管理器。

两个浏览器都把分层讲明白，而不是暗示：

- 配置文件向 Host **提供**能力；预设向 agent **暴露**能力，因此一个启用的预设行并不能证明其
  背后的能力存在
- 凡是能从 Harness 状态证明这层关联的地方——某一行指名了一个已挂载注册表并不提供的提供方——
  `/plugins` 会标注出来；证明不了的地方，它什么也不声称
- bundle 变更改变的是**下一个** Host 组合什么，因此 `/profiles` 对运行中的配置文件报告
  `restart required`，对其他配置文件指出启动命令，并且绝不提供切换已组合 Host 的 bundle 层

预设仍待完成：

- 比逐行窄切换更丰富的预设编写方式，前提是 Harness 将来暴露一个——当前的切换是最小的安全文件
  编辑，因为还没有更窄的 Harness seam
- 用不止一行原因来呈现预设自身的健康/损坏状态，前提是 Harness 的诊断将来能给出

### 6. Attachments（附件）

在能力支持时，把终端手势演化为真正的 Harness 附件：

- 通过 Harness 附件基础设施的图片与文件附件 UI
- 持久、授权的引用，而不是 base64 或 TUI 专用持久化
- 只在 `@path` 是恰当的 Harness 支持含义之处，让它演化为附件手势，而不是假装文本补全附加了数据

### 7. 权限与审批

暴露 Harness 定义的权威；不要发明前端策略。有用的人类控制需要所属能力的生命周期、授权、调度与模型感知约定——Work 演示的正是同一条规则。

### 8. 更多异步能力

在相关上游约定就绪后，呈现更多 Harness 拥有的异步工作：

- 持久终端
- workflows（工作流）——已完成：`/work` 呈现 Harness 工作流运行、它们的阶段与已发布的成员，
  所有权通过本会话自己的持久 `tool-workflow/*` 记录确立
- 之后，当 Agent Teams 的上游约定足够成熟时

### 9. TUI 可扩展性

只有在几个内部能力适配器证明了生命周期、权威与布局的词汇之后，dshline 才应考虑公共贡献 API。可能的小型 `dshline-api` 是未来的选项，而不是当前的承诺。`TuiSlots` 与浮层仍是 1.0 之前实验性的，而持久的第三方行首先需要全局活动区域布局预算。

## 健壮性就是能力工作

功能数量不值得破坏终端模型。持续优先的事项：

- Windows 真实终端验证
- Ghostty 之外更广的 macOS 终端覆盖
- Linux PTY 覆盖
- 调整大小压力测试与窄终端行为
- 恢复生命周期正确性
- 拆除与终端恢复
- Unicode、CJK 与宽字符正确性
- 与快速变化的 Harness 发布的兼容性

## 上游兼容性策略

激进跟踪、窄口支持：一次只支持一个当前的 Harness 架构。这是对兼容矩阵的刻意拒绝。目标不是最大化 Harness 版本覆盖，而是当前能力、当前性能、原生 Harness API、较小的维护面，以及快速的向前迁移。

已采纳的架构就是 `HARNESS_TARGET` 中的一个提交与一个版本，并由三个信号保证其诚实：

1. **已采纳目标**——一个确切的上游修订版，从源码检出并且阻塞，因为它就是 dshline 声称支持的东西；
2. **权威发布发现**——Harness-Sync 盯住上游官方的 `dsh-v*` GitHub Release，也就是 `HARNESS_TARGET` 唯一能记录的东西，并开启一个 pull request 提出下一代。它不读源码，也不判断兼容性：判断由那个 pull request 上的常规检查完成。`master` 上的任意提交并不是一个采纳单元，因此不再有任何东西跟随该分支；以及
3. **已发布的 npm 分发**——真实用户安装所经由的路径，它落后于 GitHub 源码，这也是预期之中的。

当上游破坏了我们时，应对方式是向前迁移并在一次提交中删除旧假设——绝不是兼容适配器、运行时特性检测，或一个承诺两代的 peer 范围。dshline 明确不承诺的是：任何更旧的预发布代仍然可用。

## 当前限制

- **主题只影响新的行。**`/theme` 在五套随包发布的配色中选择并重绘活动区域；已提交到原生滚动缓冲区的行保留它们被打印时的颜色，因为已提交的输出永不重写。应用一套配色会以一行用新配色绘制的确认作为回应。尚不支持用户自定义配色：主题所依据的角色词汇仍然是内部的，在真实配色充分检验它之前就公开它，等于冻结一份未经验证的契约。
- **`ctrl-o` 只影响新输出。**已提交的原生滚动缓冲区永不重新格式化；被截断的工具卡片反而可以打开一个有界检视器，任意细节级别、行预算远大于卡片本身，而在检视器内部 `ctrl-o` 会退回浏览最近十二张。再往前，已提交行旁边的省略标记就是仅剩的答案。
- **`/connect` 只能通过 Harness 的 pi-ai 配置领域声明自定义路由**，与 Harness 自己的 Models 界面所公开的范围一致。网关、自托管服务器或本地 OpenAI 兼容端点可以从「+ 添加自定义提供方」添加，因为 `llm-pi-ai` 的设置配置文件能够描述整条路由。其他适配器家族仍然只限于它们的可配置提供方目录已经发布的路由，除非它们获得一个等效的声明约定。
- **`/connect` 精选的是一条路由的端点、协议与模型目录，而不是它整份设置配置文件。**进阶的 pi-ai 字段——`compat`、请求头、重试策略、逐模型推理——仍然留在 `settings.yaml` 中；编辑 Connect 所呈现的内容永远不会破坏它未渲染的字段。
- **遗忘登录是本地的。**Harness 没有让提供方声明服务端撤销的地方，因此删除凭据记录不会告知签发方。
- **`@path` 插入文本，而不是附件。**补全为模型命名一个要读取的路径；它不附加其内容。
- **工具调用默认不审查。**Harness 部署决定沙箱与审批策略；见[使用 → 权限与沙箱](docs/usage.zh.md#permissions-and-the-sandbox)。
- **目标可以在没有 `/goal` 命令的情况下启动。**`/goal <objective>` 启动一次 Harness 目标驱动器运行，而 Harness 还把 `create_goal` 作为模型可调用的工具发布，它可以从普通请求推断意图。无论哪种方式，只要目标存活，状态行就点名目标；在让目标运行着离开前，检查或暂停它。
- **每窗口一次一个会话。**`/sessions` 在原位重新打开会话，而不是在现有会话旁边；没有标签页、分屏或并排 agent。
- **重新打开等待安静。**一轮进行中，或任务或 subagent 附着在被离开的会话上时，窗口拒绝重新打开会话，因为没有通用 seam 定义其所有者被退役时工作会怎样。
- **内容搜索取决于部署。**全文会话搜索是会话查询引擎的抽象接口；未实现它的后端会让 `tab` 报告这一点，过滤仍然可用。
- **已开始的会话不能实时切换预设。**Harness 在 `agentPresets.select()` 内部拒绝它；`/plugins` 读取同一个 `turnBoundary` 投影以避免提供不可能的动作，并改为提供下一个会话的默认项。
- **`/plugins` 直接编辑预设的组合文件。**切换某一行是对 `agent.cordis.yml` 的一次窄范围、带锁协调的编辑，因为 Harness 还没有暴露更细粒度的变更约定；条件（`!!js`）行绝不被求值或切换，只被报告。
- **预设出现之前的会话，其组合只能近似。**在 dshline 采纳 agent 预设之前产生的会话没有记录预设，因此按随附的 `standard` 恢复——这个预设的含义正是它们当初实际运行的那套扁平工具集。在未提供可用 `standard` 的部署上，恢复回退到该部署自己的默认值，并由 transcript（文本记录）说明工具可能与这段历史产生时所用的不同。此后创建的会话自带预设，无需猜测。
- **`/profiles` 不能切换运行中的 Host。**配置文件的 bundle 层在启动时组合一次，Harness 没有暴露能在活动进程下重新链接它们的 seam。另一个配置文件会被呈现，并指出启动它的命令；安装、更新或移除某个 bundle 报告的是它为下一个 Host 改变了什么，而不是假装能触及这一个。
- **bundle 操作需要一个可解析的 `dsh` 启动器。**它们被转发给 `dsh plugin --profile <name> …` 而不是重新实现，启动器的查找方式与 `bin/dshline.mjs` 相同的四种（`DSH_BIN`、`DSH_HARNESS` 检出、`PATH`、已安装的 `@deepseek-ai/dsh`）。四种都解析不出时，会指出确切命令，而不是让操作静默失败。
- **能力健康是可用性，不是安装状态。**`/plugins` 会标注这样一行：它已启用，但它指名的提供方是已挂载的 Host 注册表并不提供的；它说的是该提供方在这个 Host 中不可用——这正是注册表能证明的。它的包是否安装是另一回事，本前端不读取。
  链接表未覆盖的能力模块、从不被求值的 `!!js` 提供方，以及未挂载此类注册表的配置文件，都不产生任何判定——没有警告并不等于声称某一行可以工作。
- **Linux 与 macOS 已验证；Windows 未验证。**macOS 这边的证据是 Ghostty，因此另一个 macOS 终端很可能没问题，但未经证明；而 Windows 终端行为根本没有任何真实终端证据。

## 明确的非目标

- 另一个 agent 运行时
- 另一个任务运行时
- 提供方专用的 subagent 引擎
- 解析渲染后的工具输出以获取结构化状态
- 替换 Harness 持久化或会话搜索
- 替换原生终端滚动缓冲区
- 逐特性克隆 Claude Code 或 Codex
- 在内部适配器证明一个之前，发布稳定的公共 TUI SDK
- npm 式的插件市场或包管理器（`dsh plugin --profile <name> add/remove` 已经在配置文件层面覆盖了这一点）