---
"@dshline/dshline": patch
---

Total the spans hidden behind the timing panel's elision row (`… +3 more · 15.0s`). Small spans sort beneath long ones but add up, so the hidden tail can outweigh every row shown, and a bare count said nothing about what it hid. The total is dropped whole on narrow terminals rather than cut into a broken duration.
