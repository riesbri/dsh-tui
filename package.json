{
  "name": "dsh-tui-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": "^22.19 || >=24"
  },
  "scripts": {
    "build": "tsc -b packages/renderer && pnpm --filter @riesbri/dsh-tui run build",
    "clean": "tsc -b --clean && rm -rf packages/tui/lib",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "@xterm/headless": "^6.0.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
